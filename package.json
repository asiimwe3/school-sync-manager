{
  "name": "school-sync-manager",
  "private": true,
  "workspaces": ["apps/*", "lib/*", "packages/*"],
  "scripts": {
    "dev":   "turbo run dev",
    "build": "turbo run build",
    "test":  "turbo run test",
    "lint":  "turbo run lint"
  },
  "devDependencies": {
    "turbo":      "^2.0.0",
    "typescript": "^5.4.0",
    "vitest":     "^1.6.0"
  }
}
